
// import { useQuery } from '@tanstack/react-query'

// export const useAMIGOS = (tab: number) => {
//   const endpoint = tab === 0 ? '/api/AMIGOS' : tab === 1 ? '/api/sugestoes' : '/api/procurar'
//   return useQuery(['AMIGOS', tab], () =>
//     fetch(endpoint).then((res) => res.json())
//   )
// }
